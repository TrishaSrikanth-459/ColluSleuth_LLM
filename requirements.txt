ag2[openai]>=0.3.0
python-dotenv>=0.19.0
pydantic>=2.0.0
pytest>=7.0.0
aiohttp
tenacity
bandit>=1.7.5
datasets>=2.14.0
# strace is a system tool, not a Python package – ensure it's installed on the host